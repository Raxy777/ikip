"""ikip-provenance — every derived artifact and answer records where it came from.

Given a source document + processing versions, this package stamps derivatives so that:
  - an answer can be reproduced from the exact config that produced it,
  - a correction can enumerate every affected artifact,
  - a deletion can enumerate every derivative to purge.
"""

__all__: list[str] = []
