[project]
name = "ikip-observability"
version = "0.0.0"
description = "Structured logging, metrics, and tracing helpers shared across services."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ikip_observability"]
