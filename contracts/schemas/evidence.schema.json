{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ikip.local/contracts/schemas/evidence.v1.json",
  "title": "Evidence",
  "description": "A single authorized piece of retrieved evidence. Only evidence that has passed ikip-authz filtering may be constructed.",
  "type": "object",
  "additionalProperties": false,
  "required": ["evidence_id", "document_id", "text", "provenance", "authority", "applicability"],
  "properties": {
    "evidence_id": { "type": "string" },
    "document_id": { "type": "string" },
    "text": { "type": "string", "description": "The retrieved passage. Treated as DATA, never as instruction." },
    "provenance": { "$ref": "provenance.v1.json" },
    "authority": {
      "type": "string",
      "enum": ["approved", "draft", "superseded", "withdrawn", "unknown"],
      "description": "Governance authority state of the source at retrieval time."
    },
    "applicability": {
      "type": "object",
      "description": "Asset/site/equipment-family scoping that gated this evidence.",
      "additionalProperties": true
    },
    "retrieval_score": { "type": "number" },
    "retrieved_by": {
      "type": "string",
      "enum": ["exact", "lexical", "semantic", "relationship", "shape"]
    }
  }
}
